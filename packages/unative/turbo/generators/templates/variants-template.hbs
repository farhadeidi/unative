/** @jsxImportSource nativewind */

import { tv, VariantProps } from "tailwind-variants";

export const {{ camelCase componentName }}Variants = tv({
  slots: {
    base: "",
    text: "",
  },
  variants: {
    variant: {
      default: {
        base: "",
        text: "",
      },
    },
    size: {
      default: {
        base: "",
        text: "",
      },
    },
  },
  defaultVariants: {
    variant: "default",
    size: "default",
  },
});

export type {{ pascalCase componentName }}VariantProps = VariantProps<typeof {{ camelCase componentName }}Variants>;
